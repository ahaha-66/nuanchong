# 暖宠技术逻辑方案

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 暖宠技术逻辑方案 |
| 文档版本 | V1.1 |
| 对应产品文档 | 暖宠宠物连续照护平台 PRD V3.0 |
| 目标阶段 | MVP验证、首阶段研发及后续扩展基线 |
| 目标读者 | 产品、前端、后端、AI、测试、运维、安全、兽医顾问 |
| 核心终端 | 宠主微信小程序、医院Web后台 |

---

## 2. 目标、假设与非目标

### 2.1 技术目标

1. 支撑“照护计划—任务执行—状态记录—时间线—AI摘要—医院复诊”的完整闭环；
2. 在多病种下复用同一套照护领域模型，通过模板配置差异；
3. 确保用药执行、家庭协作和计划版本具备可追溯性；
4. 将生成式AI限制在结构化、整理和表达层，医疗判断由规则、专业模板和人工确认控制；
5. 满足用户授权、医院多租户、敏感数据隔离和审计要求；
6. 保持MVP实现简单，同时为医院系统、硬件和多模态能力预留扩展点。

### 2.2 关键工程假设

- MVP阶段并发规模不高，业务复杂度高于流量复杂度；
- 微信小程序是宠主主入口，医院后台是B端入口；
- 医院不承担默认实时监控责任，系统主要提供异步随访和复诊信息；
- 医院创建的计划必须经人工确认，AI不能直接下发医疗计划；
- 用户上传的语音、图片和视频属于敏感数据，需要私有存储和受控访问；
- 初期不依赖医院HIS/PIMS深度集成，可通过二维码、链接和人工录入工作；
- 初期不训练自有医学模型，使用可替换的语音、多模态及大语言模型服务。

### 2.3 技术非目标

- 不建设在线首诊、处方开具和远程诊疗系统；
- 不建设完整医院HIS/PIMS；
- 不实现AI疾病诊断或自动调整药物剂量；
- 不在MVP阶段拆分为大量微服务；
- 不实现全量智能硬件协议；
- 不构建实时视频监控和全天候医院告警中心；
- 不建设商城、保险交易和社区系统。

---

## 3. 总体技术原则

### 3.1 模块化单体优先

MVP采用模块化单体架构，而不是微服务。所有业务模块位于同一后端应用，但通过明确模块边界、领域事件和独立数据访问层解耦。

原因：

- 当前核心风险是需求与工作流，不是系统吞吐；
- 计划、任务、记录、授权和AI结果存在强事务关联；
- 单体部署、测试和观测成本更低；
- 后续可按负载和团队边界拆分AI、通知、报告和媒体模块。

### 3.2 确定性逻辑与生成式AI分离

- 任务生成、用药防重复、权限、趋势统计、紧急规则必须使用确定性代码；
- AI只负责非结构化内容提取、文本整理和自然语言摘要；
- 所有AI输出先经过结构校验、业务规则校验和安全过滤；
- 医疗相关AI结果必须能够回溯原始记录。

### 3.3 所有关键变化可追溯

照护计划版本、药物剂量、任务执行、记录修正、医院访问、授权撤销及AI生成结果均需保留审计记录。

### 3.4 用户确认优先

语音、OCR和多模态识别结果不直接进入正式医疗相关时间线，必须由用户或医院成员确认。

### 3.5 配置适配病种

疾病差异通过模板、指标定义、提醒规则和任务参数配置，不为每个病种复制代码流程。

---

## 4. 总体架构

```mermaid
flowchart TB
    subgraph Clients[客户端]
        MP[微信小程序]
        WEB[医院Web后台]
    end

    subgraph Edge[接入层]
        CDN[CDN/WAF]
        API[API Gateway / BFF]
        WX[微信登录与订阅消息]
    end

    subgraph App[暖宠后端：模块化单体]
        IAM[身份与权限]
        PET[宠物与照护圈]
        PLAN[模板与照护计划]
        TASK[任务与执行]
        OBS[状态观察与时间线]
        HOSP[医院与随访]
        CONSENT[授权与审计]
        AI[AI编排与安全]
        REPORT[周报与复诊报告]
        NOTICE[通知调度]
    end

    subgraph Data[数据与基础设施]
        PG[(PostgreSQL)]
        REDIS[(Redis)]
        OBJ[(对象存储)]
        QUEUE[任务队列]
        O11Y[日志/指标/链路]
    end

    subgraph External[外部服务]
        LLM[大语言/多模态模型]
        ASR[语音识别]
        SMS[短信服务]
        PIMS[医院系统/硬件接口]
    end

    MP --> CDN --> API
    WEB --> CDN
    API --> App
    WX --> IAM
    App --> PG
    App --> REDIS
    App --> OBJ
    TASK --> QUEUE
    NOTICE --> QUEUE
    AI --> LLM
    AI --> ASR
    NOTICE --> WX
    NOTICE --> SMS
    HOSP -.后续.-> PIMS
    App --> O11Y
```

### 4.1 客户端

#### 宠主端

推荐使用微信小程序，支持微信授权登录、订阅消息、二维码绑定医院和家庭分享。

#### 医院端

推荐使用响应式Web后台，主要适配桌面浏览器，护士或客服可在平板上使用。

### 4.2 接入层

- 统一HTTPS；
- WAF、限流、请求签名和IP风控；
- 小程序与医院端使用不同BFF视图，但复用领域服务；
- API使用版本前缀，例如 `/api/v1`；
- 所有写请求携带幂等键或业务版本号。

### 4.3 应用层

应用层按领域模块组织，不允许跨模块直接读写对方表。跨模块协作优先使用领域服务和事务内事件。

### 4.4 数据层

- PostgreSQL：关系数据、事务和审计；
- Redis：短期锁、幂等键、缓存和限流；
- 对象存储：图片、语音、视频、报告文件；
- 队列：通知、报告生成、AI任务和周期性任务；
- 可观测平台：结构化日志、错误追踪、指标和告警。

---

## 5. 推荐技术栈

### 5.1 前端

| 场景 | 建议 |
| --- | --- |
| 微信小程序 | Taro + React + TypeScript，或团队熟悉时采用原生小程序 |
| 医院后台 | React + TypeScript + Vite + Ant Design |
| 数据请求 | TanStack Query，统一错误和缓存策略 |
| 表单 | React Hook Form + Schema校验 |
| 图表 | ECharts，避免前端自行计算医疗趋势 |

选择Taro的前提是团队已有React经验；若只发布微信且重视首屏性能，可直接使用原生小程序。

### 5.2 后端

| 能力 | 建议 |
| --- | --- |
| 应用框架 | NestJS + TypeScript |
| API | REST/OpenAPI，内部异步采用领域事件 |
| ORM | Prisma或TypeORM，统一迁移脚本 |
| 数据库 | PostgreSQL 16+ |
| 缓存/锁 | Redis |
| 队列 | BullMQ/Redis；规模扩大后可切换云队列 |
| 文件 | 阿里云OSS、腾讯云COS或同等私有对象存储 |
| 身份认证 | 微信OpenID/UnionID + 医院账号OIDC/JWT |

### 5.3 AI服务

MVP可由后端AI模块直接编排模型API。达到以下任一条件后拆为独立Python/FastAPI服务：

- 需要复杂多模型路由；
- 需要批量向量或时序特征计算；
- 需要独立扩缩容；
- AI团队与业务后端由不同团队维护。

模型调用必须通过Provider Adapter，业务代码不得绑定单一厂商。适配接口至少包括：

```text
transcribeAudio()
extractObservation()
summarizeTimeline()
generateFollowupQuestions()
analyzeImageMetadata()
```

### 5.4 部署

- Docker容器化；
- MVP使用托管容器或单集群Kubernetes均可，优先选团队熟悉方案；
- 数据库、Redis和对象存储使用云托管服务；
- 国内用户数据优先部署在中国境内区域；
- 区分开发、测试、预发布和生产环境；
- 密钥存储在Secrets Manager，不进入代码和镜像。

---

## 6. 领域模块边界

| 模块 | 核心职责 |
| --- | --- |
| Identity | 用户、登录会话、设备和基础身份 |
| Organization | 医院租户、部门、员工和角色 |
| Pet | 宠物档案、主人关系和照护圈 |
| Template | 病种模板、指标定义、适用范围和审核版本 |
| CarePlan | 计划实例、版本、来源、状态和计划变更 |
| Task | 任务定义、任务实例、完成、跳过和异常 |
| Observation | 状态记录、测量值、媒体和用户确认 |
| Timeline | 汇聚各模块事实事件，提供查询视图 |
| Collaboration | 我来处理、临时权限和防重复操作 |
| HospitalCare | 患者绑定、随访问卷、工作队列和复诊 |
| Consent | 数据授权、授权范围、撤销和访问判定 |
| AIOrchestrator | 语音/多模态提取、摘要、安全校验和模型版本 |
| Report | 周报、复诊摘要、只读分享和PDF |
| Notification | 订阅消息、短信、站内信和重试 |
| Audit | 关键操作、访问记录和合规导出 |
| Analytics | MVP事件采集、指标口径、试点分组和付费实验 |

---

## 7. 核心领域模型

### 7.1 实体关系概览

```mermaid
erDiagram
    USER ||--o{ PET_MEMBER : joins
    PET ||--o{ PET_MEMBER : has
    PET ||--o{ CARE_PLAN : owns
    HOSPITAL ||--o{ HOSPITAL_STAFF : employs
    HOSPITAL ||--o{ CARE_TEMPLATE : owns
    CARE_TEMPLATE ||--o{ TEMPLATE_VERSION : versions
    CARE_PLAN ||--o{ PLAN_VERSION : versions
    PLAN_VERSION ||--o{ TASK_DEFINITION : defines
    TASK_DEFINITION ||--o{ TASK_INSTANCE : schedules
    TASK_INSTANCE ||--o{ TASK_EXECUTION : executes
    PET ||--o{ OBSERVATION_RECORD : records
    OBSERVATION_RECORD ||--o{ MEDIA_ASSET : attaches
    PET ||--o{ TIMELINE_EVENT : aggregates
    PET ||--o{ CONSENT_GRANT : authorizes
    HOSPITAL ||--o{ CONSENT_GRANT : receives
    PET ||--o{ REPORT : generates
    USER ||--o{ AUDIT_LOG : acts
```

### 7.2 关键实体

#### User

```text
id, wechat_open_id, union_id, mobile, display_name,
status, created_at, last_login_at
```

手机号、OpenID等标识单独加密或脱敏展示。用户注销采用业务删除和法定保留策略组合。

#### Pet

```text
id, owner_user_id, name, species, breed, sex,
birth_date, weight, health_stage, care_timezone, status,
primary_hospital_id, created_at, updated_at
```

`health_stage`用于产品展示，不作为医疗诊断字段。`care_timezone`用于任务生成、逾期判断和报告自然日边界；修改时只影响尚未生成的任务，已生成任务保留原计划时间及原时区快照。

#### PetMember

```text
id, pet_id, user_id, role, permission_set,
invited_by, valid_from, valid_until, status
```

角色建议：OWNER、FAMILY、TEMP_CARER、VIEWER。实际权限由permission_set决定，避免只靠角色硬编码。

#### CareTemplate / TemplateVersion

```text
template_id, hospital_id, category, disease_tag,
version, content_schema, applicable_conditions,
contraindications, reviewer_id, review_status,
published_at, retired_at
```

模板发布后不可原地修改，只能创建新版本。旧计划继续引用创建时版本。

#### CarePlan / PlanVersion

```text
care_plan_id, pet_id, source_type, source_org_id,
current_version_id, status, start_at, end_at,
created_by, created_at, updated_at

plan_version_id, version_no, template_version_id,
status, plan_payload, change_reason, created_by,
confirmed_by, confirmed_at, effective_at,
supersedes_version_id, created_at
```

来源类型：USER、HOSPITAL、IMPORT_ASSISTED。AI/OCR只能生成草稿，不能作为正式来源。

`CarePlan.status`只表示计划容器生命周期：ACTIVE、PAUSED、COMPLETED、CANCELLED。`PlanVersion.status`表示内容版本生命周期：DRAFT、PENDING_CONFIRMATION、ACTIVE、REJECTED、SUPERSEDED。任一计划最多只有一个ACTIVE版本和一个待确认版本。

用户自主创建的计划经保存确认后可直接激活；医院或导入辅助产生的版本必须由主人确认。用户修改医院版本时不原地改写医院内容，而是创建来源为USER的新计划或用户覆盖版本，并明确展示“医院原计划”和“用户当前执行计划”。药物、剂量、频率、停药时间及冲突项必须二次确认并审计。

#### TaskDefinition

```text
id, plan_version_id, task_type, title,
schedule_rule, valid_window, payload,
priority, completion_schema, reminder_policy,
observation_definition_ids
```

`payload`按task_type使用不同JSON Schema，例如药物任务包含药名、剂量、单位和给药方式。

当前用药不是独立可随意编辑的字段，而是由所有ACTIVE计划版本中的药物TaskDefinition投影生成`CurrentMedicationProjection`，保留计划来源、版本、剂量、频率、起止时间和冲突标记。报告展示“当前用药”时必须注明来源；不同计划冲突时并列展示，不自动完成医疗意义上的用药核对。

#### TaskInstance

```text
id, task_definition_id, pet_id, scheduled_at,
window_start, window_end, status, claimed_by,
claim_expires_at, version, generated_key,
schedule_timezone, cancelled_reason
```

`generated_key`由任务定义、宠物和计划时间生成唯一键，防止调度器重复创建。

#### TaskExecution

```text
id, task_instance_id, executor_user_id,
action, actual_at, result_payload,
note, created_at, supersedes_execution_id,
related_execution_id, confirmation_context
```

任务执行记录采用追加写，不覆盖历史；更正通过supersedes关联。

`action`至少包括COMPLETE、SKIP、ABNORMAL、CORRECT和REPEAT_EXECUTION_CONFIRMED。普通完成在数据库层保证每个任务只有一个当前有效执行；用户确认已完成用药后的再次操作时，新增REPEAT_EXECUTION_CONFIRMED记录，不覆盖第一次给药，并触发醒目的重复给药提示和审计。

#### ObservationDefinition

```text
id, definition_key, name, value_type, allowed_units,
allowed_range, enum_options, normal_rule,
recording_frequency, display_priority, source_template_version_id,
status, schema_version
```

指标定义承载默认指标和模板扩展指标。任务只引用定义快照；模板升级不能改变历史记录含义。NORMAL、ABNORMAL是已记录结果，NOT_RECORDED是按应记录窗口计算出的数据完整度状态，不以空值或伪造记录表示。

#### ObservationRecord

```text
id, pet_id, definition_key, observed_at,
value_type, value_number, value_text, value_enum,
unit, source_type, confidence, confirmed_by,
raw_content_id, definition_snapshot, status,
hidden_at, hidden_by, created_by, created_at
```

必须明确区分NORMAL、ABNORMAL和NOT_RECORDED，不能用空值代表正常。隐藏只影响普通时间线和摘要展示，不物理删除事实；用户可恢复，医院端不得绕过授权读取隐藏记录。

#### ClinicalEvent

```text
id, pet_id, event_type, occurred_at, ended_at,
title, structured_payload, source_type, source_org_id,
media_refs, created_by, correction_status, created_at
```

`event_type`覆盖就诊、检查、手术、换药、换粮、随访、复诊和计划调整。ClinicalEvent保存事实，TimelineEvent仅作为可重建查询投影。

#### TimelineEvent

```text
id, pet_id, occurred_at, event_type,
source_module, source_id, summary,
visibility, correction_status
```

时间线是查询投影，不是所有业务事实的唯一存储。事实仍保存在各领域表中。

#### ConsentGrant

```text
id, pet_id, grantor_user_id, grantee_org_id,
scope, data_types, metric_keys, media_access,
start_at, end_at, status, revoked_at, created_at
```

权限判断必须实时检查授权状态，不能仅依赖发放时生成的长期Token。MVP采用严格撤销：撤销后医院不能继续读取家庭数据，包括授权期内已产生但未转化为医院自有记录的数据。医院在授权有效期内依法或依约形成的随访记录、诊疗记录是医院数据，可继续保留，但必须与家庭数据分表、分权限展示。导出文件和分享链接在撤销时立即失效。

#### PatientBinding / Followup

```text
patient_binding_id, pet_id, organization_id, status,
bound_at, unbound_at, service_hours_policy_id

followup_definition_id, organization_id, title,
question_schema, trigger_rule, version, status

followup_instance_id, pet_id, organization_id,
definition_version, status, assigned_to,
scheduled_at, sent_at, submitted_at, claimed_at,
completed_at, expires_at, response_payload,
linked_report_id, linked_plan_version_id
```

绑定医院不自动授权家庭数据，也不代表实时监控。随访实例状态为SCHEDULED、SENT、SUBMITTED、CLAIMED、COMPLETED、EXPIRED、CANCELLED；只有员工主动CLAIMED后才展示为医院处理中。

#### AIArtifact

```text
id, pet_id, artifact_type, input_refs,
model_provider, model_name, prompt_version,
schema_version, raw_output, validated_output,
safety_result, user_correction, status, created_at
```

#### Report

```text
id, pet_id, report_type, period_start, period_end,
snapshot_version, content, evidence_refs,
status, formats, generated_at, share_token_hash,
share_revoked_at, expires_at, source_revision
```

报告状态为QUEUED、GENERATING、READY、FAILED、STALE、REVOKED。原始记录更正或隐藏后，将依赖该记录的READY报告标记为STALE；历史快照保留，但再次分享前必须重新生成或明确提示版本已过期。

#### AnalyticsEvent / ExperimentOrder

```text
analytics_event_id, event_name, occurred_at, actor_id,
pet_id, organization_id, session_id, properties,
product_version, experiment_group

experiment_order_id, payer_type, payer_id, plan_code,
amount_minor, status, channel, paid_at, refunded_at
```

AnalyticsEvent不得保存原始病情文本、媒体内容和模型Prompt。试点阶段允许使用手工订单或外部支付链接验证付费，不要求先建设完整订阅计费系统。

### 7.3 推荐索引

- `task_instance(pet_id, scheduled_at, status)`；
- `task_instance(task_definition_id, generated_key)`唯一索引；
- `observation_record(pet_id, definition_key, observed_at)`；
- `observation_definition(definition_key, schema_version)`唯一索引；
- `timeline_event(pet_id, occurred_at desc)`；
- `consent_grant(pet_id, grantee_org_id, status, end_at)`；
- `patient_binding(pet_id, organization_id)`唯一索引；
- `followup_instance(organization_id, status, scheduled_at)`；
- `task_execution(task_instance_id)`对当前有效普通完成建立部分唯一索引；
- `audit_log(org_id, created_at)`和`audit_log(subject_type, subject_id)`；
- 医院租户表的所有核心索引包含`organization_id`。

---

## 8. 核心状态机

### 8.1 照护计划容器

```mermaid
stateDiagram-v2
    [*] --> Active: 首个版本生效
    Active --> Paused: 暂停
    Paused --> Active: 恢复
    Active --> Completed: 周期结束
    Active --> Cancelled: 主动取消
    Completed --> [*]
    Cancelled --> [*]
```

### 8.2 计划版本

```mermaid
stateDiagram-v2
    [*] --> Draft
    Draft --> Active: 用户自主计划确认保存
    Draft --> PendingConfirmation: 医院下发/导入完成
    PendingConfirmation --> Active: 用户确认
    PendingConfirmation --> Rejected: 用户拒绝
    Active --> Superseded: 新版本生效
    Rejected --> [*]
    Superseded --> [*]
```

医院修改已生效计划时创建新版本。新版本待确认期间，旧ACTIVE版本继续执行。用户确认后，在同一事务中锁定计划、将旧版本改为SUPERSEDED、设置新版本ACTIVE及`effective_at`、更新`current_version_id`并写入Outbox事件。

任务切换规则：

- `scheduled_at < effective_at`的旧任务保留原状态；
- `scheduled_at >= effective_at`且尚未执行的旧任务标记CANCELLED，原因记录为PLAN_VERSION_CHANGED；
- 已完成、跳过、异常及重复执行记录永不回滚；
- 新版本从`effective_at`开始生成任务，唯一键包含计划版本和计划时间；
- 已被领取但尚未完成的旧任务在生效事务中取消领取并通知领取人；
- 修改药物剂量、频率、给药方式或停药时间必须二次确认，并通知所有具备用药权限的协作者。

### 8.3 任务实例

```mermaid
stateDiagram-v2
    [*] --> Pending
    Pending --> Claimed: 我来处理
    Claimed --> Pending: 领取超时
    Pending --> Completed: 完成
    Claimed --> Completed: 完成
    Pending --> Skipped: 跳过
    Pending --> Abnormal: 执行异常
    Pending --> Overdue: 超过窗口
    Pending --> Cancelled: 计划暂停/取消/换版
    Claimed --> Skipped: 领取人跳过
    Claimed --> Abnormal: 执行异常
    Claimed --> Overdue: 超过窗口
    Claimed --> Cancelled: 计划暂停/取消/换版
    Overdue --> Completed: 延迟完成
    Overdue --> Skipped: 确认跳过
    Overdue --> Abnormal: 执行异常
    Completed --> Corrected: 记录更正
    Completed --> RepeatConfirmed: 二次确认再次执行
```

`即将到期`是由当前时间、`window_end`和提醒策略计算出的展示态，不持久化为业务状态。`延迟`对应持久化状态OVERDUE。所有完成、跳过、异常和重复执行提交都要求Idempotency-Key、任务版本号及数据库条件更新。

### 8.4 模板版本

```text
DRAFT → IN_REVIEW → APPROVED → PUBLISHED → RETIRED
                   ↘ REJECTED
```

创建人与审核人默认不能是同一人；只有PUBLISHED模板可用于医院正式下发。医院下发前的个性化调整保存在计划版本快照中，不原地修改模板。模板退役不影响已创建计划。

### 8.5 AI提取结果

```text
QUEUED → PROCESSING → NEEDS_CONFIRMATION → CONFIRMED
                  ↘ FAILED / SAFETY_BLOCKED / EXPIRED
```

只有CONFIRMED状态可以生成正式ObservationRecord和TimelineEvent。

### 8.6 随访实例

```mermaid
stateDiagram-v2
    [*] --> Scheduled
    Scheduled --> Sent: 到达发送时间
    Sent --> Submitted: 用户提交
    Submitted --> Claimed: 医院员工领取
    Claimed --> Completed: 完成随访
    Scheduled --> Cancelled: 主动取消
    Sent --> Expired: 超过填写期限
    Submitted --> Expired: 超过处理期限且未领取
```

医院服务时间、预计响应范围和是否提供SLA由`service_hours_policy`配置。系统告警、用户异常提示、医院工作队列和员工领取状态分别存储，不能互相推导为“医院已接警”。

---

## 9. 核心业务流程

### 9.1 用户自主创建计划

```text
创建宠物并选择照护阶段
→ 创建用户来源计划和首项任务定义
→ 选择最多3个重点观察项
→ 校验时间、单位及药物必填字段
→ 用户确认保存并激活首个版本
→ 事务提交计划、版本、Outbox事件
→ 幂等生成近期任务
→ 动态照护卡可见
```

用户计划可直接激活，但药物名称、剂量、单位和频率必须逐项确认。若存在同时有效的医院计划，系统展示潜在时间或药物冲突，不自动合并或判断哪个计划正确。

### 9.2 医院下发照护计划

```mermaid
sequenceDiagram
    participant S as 医院员工
    participant P as Plan模块
    participant U as 宠主小程序
    participant T as Task模块
    participant N as 通知模块

    S->>P: 选择模板并修改
    P->>P: 校验模板版本/必填字段/员工权限
    P-->>S: 生成计划草稿
    S->>P: 确认下发
    P->>U: 创建待确认计划和绑定码
    N-->>U: 订阅消息/站内通知
    U->>P: 查看来源、内容并确认
    P->>P: 激活计划版本
    P->>T: 发布 CarePlanActivated
    T->>T: 按调度规则生成近期任务实例
```

关键规则：

- 医院员工必须属于计划来源医院并具备PLAN_WRITE权限；
- 计划确认页显示医院、员工、模板版本和更新时间；
- 模板与计划内容均保存快照，不能只保存外键；
- 用户拒绝计划时不产生正式任务；
- 任务生成采用唯一键和幂等消费。

### 9.3 多人防重复用药

```mermaid
sequenceDiagram
    participant A as 家庭成员A
    participant B as 家庭成员B
    participant API as Task API
    participant DB as PostgreSQL

    A->>API: 点击“我来处理”
    API->>DB: 条件更新PENDING且version=n
    DB-->>API: 返回CLAIMED及新version
    B->>API: 尝试完成任务
    API->>DB: 条件更新PENDING或本人CLAIMED且version=n
    DB-->>API: 0行更新
    API-->>B: A正在处理，刷新状态
    A->>API: 提交完成
    API->>DB: 校验CLAIMED且claimed_by=A
    API->>DB: 事务写TaskExecution并更新Completed
```

PostgreSQL是领取和完成状态的唯一事实源，Redis只允许作为短期性能优化。领取失败或超时可根据数据库状态恢复。防重复不能只依赖前端按钮状态，后端必须使用条件更新、版本号和当前有效执行唯一约束。允许具备权限的成员追加更正，但不能物理删除执行记录。

已完成用药再次操作时，接口先返回原执行人、实际时间和剂量，并要求用户明确确认“记录再次给药”。确认后追加REPEAT_EXECUTION_CONFIRMED，不把原任务重新改为Completed，也不静默覆盖第一次记录。

### 9.4 语音快速记录

```mermaid
sequenceDiagram
    participant U as 用户
    participant M as 媒体模块
    participant AI as AI编排
    participant ASR as 语音识别
    participant V as 规则校验
    participant O as Observation模块

    U->>M: 上传语音并获取media_id
    M-->>U: 上传完成
    U->>AI: 请求提取(media_id, pet_id)
    AI->>ASR: 语音转写
    ASR-->>AI: 转写文本
    AI->>AI: 按JSON Schema提取
    AI->>V: 单位/时间/禁用医疗结论校验
    V-->>U: 待确认结构化结果
    U->>O: 确认或修改
    O->>O: 写Observation和TimelineEvent
```

### 9.5 动态照护卡

动态照护卡使用服务端聚合读模型，一次返回：

- 当前照护阶段和主要健康问题；
- 当前ACTIVE计划版本、来源和更新时间；
- 今日任务、计算展示态、完成人和可执行动作；
- 按计划优先级选出的最多3个重点观察项；
- 最近变化、数据缺口和异常事件；
- 下次复诊及未完成随访。

聚合结果由领域事件更新，可短期缓存；执行任务等写操作成功后，响应必须返回最新版本并使相关缓存失效。客户端不得自行合并不同接口来决定药物任务是否可执行。

### 9.6 周报与复诊摘要

1. 报告服务按时间范围查询任务执行、观察记录、计划变更和就诊事件；
2. 趋势引擎使用确定性SQL/代码计算完成率、频次、分布、中位数和环比；
3. 生成Evidence Bundle，包含每个结论的记录ID；
4. LLM仅将结构化事实转换为自然语言；
5. 安全层检查诊断、因果、药物建议和夸大表达；
6. 保存报告快照、数据完整度、免责声明和证据引用；
7. 异步渲染小程序数据、PDF和长图，失败可单格式重试；
8. 用户确认后分享，医院仅在授权范围内查看；
9. 原记录更正、隐藏或授权撤销时，使相关分享和证据访问立即失效或标记STALE。

### 9.7 医院随访与复诊更新

```text
医院创建或触发随访实例
→ 用户收到问卷并提交
→ 进入医院待随访队列
→ 员工主动领取
→ 查看授权范围内摘要和证据
→ 记录处理结论及复诊安排
→ 需要时创建新计划版本
→ 用户确认后生效并完成闭环
```

患者面板分类是运营标签，不是医疗风险判断。首期规则：

- 新出院：存在医院标记的出院事件且在配置天数内；
- 术后/慢病：来自当前照护阶段或医院标签；
- 即将复诊：复诊时间进入配置窗口；
- 长期未更新：连续N个应记录日没有有效任务执行或观察记录；
- 执行异常：存在未处理ABNORMAL或重复给药记录；
- 撤销授权：绑定仍存在但ConsentGrant已撤销或过期。

绿色、黄色、红色仅表示队列优先级，并始终显示“非医疗风险分级”。规则阈值按医院配置并记录版本。

### 9.8 授权医院查看

每次读取执行：

```text
员工身份有效
AND 员工属于目标医院
AND 员工具有目标资源权限
AND ConsentGrant处于ACTIVE
AND 当前时间在授权窗口内
AND 数据类型位于scope中
```

查看报告、下载文件和打开媒体都写入审计日志。

权限按资源逐项求交集：

| 资源 | 必须检查 |
| --- | --- |
| 任务与观察 | 时间范围、data_types、metric_keys |
| 报告 | report权限、报告时间范围、当前授权状态 |
| 图片视频 | media_access、所属原始记录可见性 |
| 报告证据 | 报告和原始证据必须同时可见 |
| 导出与分享 | EXPORT权限、有效期、撤销状态 |

撤销事务更新ConsentGrant并发布ConsentRevoked。消费者立即撤销分享链接、短期签名URL和未完成导出任务；缓存以授权版本为键，撤销后旧缓存不可命中。医院自有随访/诊疗记录不因家庭授权撤销而删除，但不得继续展开已撤销的家庭证据。

---

## 10. AI技术逻辑

### 10.1 AI链路分层

```text
原始输入层
→ 语音转写/OCR/媒体元数据
→ 结构化提取
→ Schema校验
→ 业务规则与医疗安全校验
→ 用户确认
→ 正式记录
→ 确定性趋势计算
→ 证据包
→ 自然语言摘要
→ 输出安全校验
```

### 10.2 结构化提取Schema示例

```json
{
  "observed_at": "2026-06-21T20:00:00+08:00",
  "observations": [
    {
      "definition_key": "appetite_ratio",
      "value_number": 0.7,
      "unit": "ratio",
      "source_quote": "今天大概吃了七成",
      "confidence": 0.94
    },
    {
      "definition_key": "cough_count",
      "value_number": 3,
      "unit": "count",
      "period": "night",
      "source_quote": "晚上咳了三次",
      "confidence": 0.97
    }
  ],
  "task_claims": [
    {
      "task_type": "medication",
      "status": "user_claimed_completed",
      "source_quote": "药已经喂了",
      "requires_task_match": true
    }
  ],
  "medical_conclusions": []
}
```

“药已经喂了”不能直接完成任意任务。系统必须展示匹配到的待完成任务，由用户确认具体药物后再执行。

### 10.3 Prompt设计原则

系统提示应要求模型：

- 只提取用户明确表达的信息；
- 不补全疾病、原因、剂量或未表达的时间；
- 无法确定时返回`null`并要求确认；
- 每项结构化结果保留原文证据；
- 输出严格JSON，禁止Markdown和额外解释；
- 不输出诊断、处方、停药或紧急程度结论。

Prompt、Schema、安全规则和模型版本分别版本化。任何生产变更必须经过离线评测集和灰度发布。

### 10.4 趋势引擎

趋势计算不交给LLM。首期支持：

- 任务完成率、漏服次数、延迟分布；
- 事件频次按日/周聚合；
- 数值指标的中位数、最小值、最大值和变化比例；
- 当前窗口与上一等长窗口比较；
- 记录完整度和缺失天数；
- 计划变更前后时间窗口对齐；
- 好日子/坏日子及喜欢活动完成情况。

输出示例：

```json
{
  "metric": "cough_days",
  "current": {"from": "2026-06-15", "to": "2026-06-21", "value": 4},
  "previous": {"from": "2026-06-08", "to": "2026-06-14", "value": 1},
  "evidence_record_ids": ["obs_1", "obs_2", "obs_3", "obs_4"]
}
```

数据缺口根据ObservationDefinition的应记录窗口和任务要求计算，不能把“没有异常记录”解释为正常。相似异常仅按同一`definition_key`、标准化值和配置时间窗口聚合展示，原始记录保持独立并可逐条回溯；首期不使用LLM决定医学意义上的相似性。

原始记录被更正或隐藏时发布ObservationCorrected/ObservationHidden事件，重新计算受影响趋势，将依赖它的AIArtifact和Report标记为STALE。AI或模型供应商不可用时，文字、选择和手工数值记录仍可正常提交；周报退化为确定性统计模板，不阻断任务执行和医院随访。

### 10.5 安全分层

1. **输入层**：文件类型、大小、恶意内容和隐私检查；
2. **提取层**：严格Schema、字段白名单、置信度阈值；
3. **规则层**：单位范围、时间合理性、药物任务匹配；
4. **医疗层**：禁用诊断、因果和用药建议；
5. **输出层**：敏感词和承诺式文案检查；
6. **人工层**：用户确认、医院计划人工确认、兽医模板审核。

### 10.6 明确紧急场景

呼吸困难、持续抽搐、失去意识、大量出血等明确场景由兽医审核的规则词典和结构化输入触发统一提示。提示只建议尽快联系兽医或前往医院，不判断疾病和严重等级。

医院端不得自动显示为“医院已接警”。只有员工主动领取或医院另行购买带SLA的服务时，才能展示处理状态。

### 10.7 AI评测指标

- 字段级Precision/Recall/F1；
- 时间、单位和频次解析准确率；
- 虚构字段率；
- 医疗越界率；
- 用户修正率；
- 摘要事实一致率；
- 证据引用完整率；
- 单次调用成本和P95时延。

---

## 11. API设计

### 11.1 API约定

- REST + JSON；
- 时间统一使用ISO 8601并带时区；
- 金额使用最小货币单位；
- 数值指标同时携带unit；
- 写请求支持`Idempotency-Key`；
- 更新请求支持`version`或`If-Match`；
- 错误结构包含`code`、`message`、`request_id`和可选字段错误。

### 11.2 核心接口

```text
POST   /api/v1/auth/wechat/login
GET    /api/v1/me

POST   /api/v1/pets
GET    /api/v1/pets/{petId}
GET    /api/v1/pets/{petId}/care-card
POST   /api/v1/pets/{petId}/members/invitations
PATCH  /api/v1/pets/{petId}/members/{memberId}
DELETE /api/v1/pets/{petId}/members/{memberId}

POST   /api/v1/care-plans
POST   /api/v1/care-plans/{id}/versions
POST   /api/v1/care-plans/{id}/confirm
POST   /api/v1/care-plans/{id}/pause
POST   /api/v1/care-plans/{id}/resume
POST   /api/v1/care-plans/{id}/cancel

POST   /api/v1/templates
POST   /api/v1/templates/{id}/versions
POST   /api/v1/template-versions/{id}/submit-review
POST   /api/v1/template-versions/{id}/review
POST   /api/v1/template-versions/{id}/publish

GET    /api/v1/pets/{petId}/tasks?date=YYYY-MM-DD
POST   /api/v1/tasks/{id}/claim
POST   /api/v1/tasks/{id}/complete
POST   /api/v1/tasks/{id}/skip
POST   /api/v1/tasks/{id}/abnormal
POST   /api/v1/tasks/{id}/repeat-executions
POST   /api/v1/tasks/{id}/correct

POST   /api/v1/media/upload-tickets
POST   /api/v1/ai/extractions
GET    /api/v1/ai/extractions/{id}
POST   /api/v1/ai/extractions/{id}/confirm

POST   /api/v1/pets/{petId}/observations
PATCH  /api/v1/observations/{id}
POST   /api/v1/observations/{id}/hide
POST   /api/v1/observations/{id}/restore
POST   /api/v1/pets/{petId}/clinical-events
GET    /api/v1/pets/{petId}/timeline
GET    /api/v1/pets/{petId}/trends

POST   /api/v1/pets/{petId}/reports
GET    /api/v1/reports/{id}
POST   /api/v1/reports/{id}/share-links
DELETE /api/v1/reports/{id}/share-links/{linkId}

POST   /api/v1/consents
GET    /api/v1/consents
POST   /api/v1/consents/{id}/revoke

POST   /api/v1/hospitals/{id}/patient-invitations
GET    /api/v1/hospitals/{id}/patients
GET    /api/v1/hospitals/{id}/followup-queue
POST   /api/v1/followups
GET    /api/v1/followups/{id}
POST   /api/v1/followups/{id}/submit
POST   /api/v1/followups/{id}/claim
POST   /api/v1/followups/{id}/complete
```

列表接口使用游标分页并返回`data_version`。动态照护卡、任务完成、计划确认、领取和随访领取必须支持乐观并发。所有创建和动作型POST接口必须支持Idempotency-Key；同一键不同请求体返回冲突错误。

### 11.3 领域事件

```text
CarePlanActivated
CarePlanVersionChanged
TaskInstanceGenerated
TaskClaimed
TaskCompleted
TaskSkipped
TaskAbnormal
TaskRepeatExecutionConfirmed
TaskExecutionCorrected
ObservationConfirmed
ObservationCorrected
ObservationHidden
ConsentGranted
ConsentRevoked
ReportGenerated
ReportInvalidated
FollowupRequested
FollowupSubmitted
FollowupClaimed
FollowupCompleted
NotificationRequested
```

事件包含`event_id`、`occurred_at`、`actor`、`aggregate_id`、`aggregate_version`和`trace_id`。领域写入与Outbox事件必须在同一数据库事务中提交；后台发布器再投递到队列。消费者记录已处理event_id，保证幂等，避免数据库提交成功但事件丢失。

---

## 12. 通知与调度

### 12.1 任务生成

- 计划激活时预生成未来7天任务；
- 每日滚动补齐未来任务；
- 计划变更时取消未开始的旧版本任务并生成新任务；
- 已完成任务不回滚，保留版本来源；
- 时区以宠物主要照护时区为准。

### 12.2 通知策略

- 提前提醒、到点提醒、逾期提醒分开配置；
- 同一任务设置去重窗口；
- 多人照护默认不向所有人无限重复推送；
- 订阅消息不可用时降级为站内待办，关键场景可选短信；
- 每次发送记录模板、目标、结果和重试次数。

### 12.3 重试与死信

- 网络类失败指数退避；
- 业务类失败不自动重试；
- 达到最大次数进入死信队列；
- 运维后台支持查看、重放和忽略；
- 重放仍使用业务幂等键。

### 12.4 MVP埋点与指标口径

服务端业务事实优先于客户端埋点：任务完成、计划激活、授权、报告生成、随访完成和支付结果由服务端记录；页面曝光、点击和记录耗时由客户端记录。事件进入分析库前去标识化，不写入病情文本和媒体。

首期统一口径：

| 指标 | 计算口径 |
| --- | --- |
| 医院扫码激活率 | 医院邀请后7日内激活宠物数 / 有效邀请数 |
| 7/14日有效使用 | 对应窗口内至少2个不同自然日有任务执行或观察记录 |
| 28日留存 | 激活后第22—35日至少一次有效照护行为 |
| 任务完成率 | 有效完成任务数 / 到期且未取消任务数，按任务类型分层 |
| 单次记录耗时 | 打开记录表单至成功提交，排除后台和失败重试时间 |
| AI无需修改率 | AI确认时结构化字段无人工变化的记录数 / AI确认总数 |
| 周报查看率 | 报告周期内打开READY周报的活跃宠物数 / 活跃宠物数 |
| 摘要阅读时间 | 医院端前台可见且有交互的累计时长，上限截断 |
| 持续有效照护宠物 | 连续两周均有计划执行和状态记录，或完成随访—摘要—计划更新闭环 |

所有指标必须能按医院、疾病/照护组、单人/多人照护、产品版本和实验组切分。漏药与重复给药等价值指标只报告事件变化，不直接宣称医疗效果。

### 12.5 最小付费实验

阶段1支持手工订单、兑换码或外部支付链接回填，至少记录报价、实际支付、退款和权益期限。基础任务、用药提醒和防重复协作不受付费限制；完整历史、AI洞察、报告、自定义指标和多宠权益由服务端Entitlement统一判定，不能只在客户端隐藏入口。

---

## 13. 多租户、权限与数据安全

### 13.1 租户模型

医院是Organization租户。所有医院数据必须带`organization_id`，仓储层默认注入租户过滤条件，禁止由前端传入任意租户ID绕过。

家庭数据以Pet为资源边界，通过PetMember和ConsentGrant授权。医院不是宠物数据的默认所有者。

### 13.2 RBAC + ABAC

- RBAC控制医院员工角色：ADMIN、VET、NURSE、CUSTOMER_SERVICE、READ_ONLY；
- ABAC检查医院、宠物、授权范围、数据类型、时间窗口和资源来源；
- 高风险操作需要二次验证和更高权限；
- 临时照护者权限自动过期。

家庭权限至少拆分为PET_VIEW、PLAN_WRITE、MEDICATION_EXECUTE、OBSERVATION_WRITE、MEDIA_VIEW、MEMBER_MANAGE、REPORT_SHARE和CONSENT_MANAGE。只有OWNER可转移所有权、管理医院授权和执行账户级导出/删除；临时照护者默认无成员管理、授权和报告分享权限。成员撤销和到期必须使会话中的资源权限立即失效。

### 13.3 数据分类

| 等级 | 示例 | 控制 |
| --- | --- | --- |
| L1公开 | 产品帮助、模板公开说明 | 常规访问 |
| L2内部 | 运营配置、非敏感统计 | 员工权限 |
| L3敏感 | 宠物照护记录、医院患者关系 | 加密、授权、审计 |
| L4高敏感 | 手机号、OpenID、精确位置、原始媒体 | 字段加密、最小访问、严格审计 |

### 13.4 安全控制

- TLS 1.2+；
- 数据库和对象存储静态加密；
- 对象存储默认私有，使用短期签名URL；
- 生产日志不记录Token、手机号和原始病情文本；
- 敏感字段脱敏展示；
- 登录、授权、下载和媒体访问进行风控；
- 管理后台开启MFA；
- 文件上传执行扩展名、MIME、魔数、大小和恶意文件扫描，处理前保持隔离状态；
- 定期权限回收和密钥轮换；
- 数据导出和删除采用异步任务并记录审计。

### 13.5 数据保留

保留期限必须由法务、医院合同和产品策略确认。建议区分：

- 账户数据；
- 家庭照护记录；
- 医院依法/依约保存的医疗记录；
- 原始媒体；
- AI中间结果；
- 审计日志和安全日志。

用户撤销医院授权不等于删除医院依法保留的记录，两者必须在产品文案和数据模型中区分。

---

## 14. 非功能需求

### 14.1 性能目标

| 场景 | 目标 |
| --- | --- |
| 今日任务查询 | P95 < 500ms |
| 任务完成提交 | P95 < 800ms |
| 时间线首屏 | P95 < 800ms |
| 普通后台列表 | P95 < 1s |
| AI结构化 | P95 < 12s，异步可轮询 |
| 周报生成 | 2分钟内完成 |

### 14.2 可用性与恢复

- MVP月可用性目标99.5%，商业化后99.9%；
- PostgreSQL每日全量备份并持续增量备份；
- 建议RPO不超过15分钟，RTO不超过4小时；
- 对象存储开启版本控制或回收站；
- 定期执行恢复演练，而不是只检查备份任务成功。

### 14.3 可观测性

- 每个请求生成request_id和trace_id；
- 关键业务指标、技术指标和安全指标分开监控；
- 监控任务生成失败、通知积压、AI失败率、报告延迟、授权拒绝和异常登录；
- 告警必须包含影响范围、关联版本和处理手册链接。

### 14.4 无障碍与适老化

- 字号和点击区域满足移动端可用性；
- 不只用颜色表达任务状态；
- 重要用药信息避免过小文本；
- 支持家庭成员代操作，但明确操作人；
- 错误提示说明如何恢复，而非仅显示失败。

---

## 15. 医院系统与硬件集成

### 15.1 MVP阶段

- 医院通过后台人工创建或套用模板；
- 使用二维码/邀请链接绑定宠物；
- 检查报告由用户或医院上传；
- 不依赖医院现有系统即可完成闭环。

### 15.2 标准集成层

后续建立Integration Adapter：

```text
HospitalAdapter
  - importPatient()
  - importVisit()
  - importPrescriptionDraft()
  - pushFollowupSummary()

DeviceAdapter
  - syncActivity()
  - syncWeight()
  - syncFeeding()
  - syncLitterEvent()
```

所有外部数据必须记录provider、external_id、采集时间和同步时间。外部系统更新采用upsert与版本检查，不能覆盖用户手工修正而无留痕。

### 15.3 OCR导入

OCR只生成草稿：

```text
上传处方/出院说明
→ OCR文本
→ AI提取候选药物与任务
→ 单位和剂量规则检查
→ 医院员工或用户逐项确认
→ 创建正式计划草稿
```

药物名称相似、单位缺失、手写不清和剂量超出配置范围时必须阻断自动确认。

---

## 16. 测试策略

### 16.1 单元测试

重点覆盖：

- 调度规则和跨日时区；
- 任务幂等生成；
- 计划版本切换；
- 用户计划与医院计划冲突提示；
- 多人并发完成任务；
- 已完成用药二次确认与重复执行追加写；
- 授权范围判断；
- 授权撤销后的报告、证据、媒体和缓存失效；
- 趋势计算；
- 紧急规则和禁用AI输出；
- 数据更正和审计。

### 16.2 集成测试

- PostgreSQL事务与唯一约束；
- Redis锁失效和重入；
- Outbox提交、发布和消费者幂等；
- 队列重试与死信；
- 对象存储签名URL；
- 微信登录和订阅消息；
- 模型Provider超时、限流和降级。

### 16.3 端到端测试

至少覆盖：

1. 医院下发计划—用户确认—任务生成；
2. 家庭成员领取—另一成员被阻止重复执行；
3. 语音记录—AI提取—用户修正—时间线生成；
4. 授权医院—查看报告—撤销授权—再次访问被拒绝；
5. 计划改剂量—用户重新确认—新任务生效；
6. 周报生成—证据引用—分享链接过期。
7. 用户自主创建计划—设置观察项—动态照护卡展示；
8. 已完成用药再次操作—二次确认—追加重复执行记录；
9. 随访问卷—用户提交—员工领取—完成—更新计划；
10. 原始记录隐藏/更正—趋势重算—旧报告标记STALE；
11. 授权撤销—报告、媒体、分享链接和缓存访问均被拒绝；
12. 指标事件可重算出PRD规定的试点成功指标。

### 16.4 AI离线评测集

建立脱敏且经授权的固定评测集，包含：

- 正常语音、口语、省略和方言样本；
- 时间模糊、单位缺失和多药物样本；
- 正常状态、异常状态和未记录区分；
- 容易诱发诊断或因果推断的样本；
- 紧急词、否定词和历史描述；
- 恶意Prompt注入和无关内容。

模型或Prompt升级必须达到既定阈值，且医疗越界率不能回退。

### 16.5 安全测试

- 越权访问和租户穿透；
- IDOR对象直接引用；
- 分享链接枚举；
- 文件上传类型伪造；
- Token重放和会话撤销；
- 敏感信息日志泄露；
- 依赖漏洞和容器镜像扫描。

---

## 17. 建议代码结构

```text
warm-pet/
  apps/
    miniapp/                 # 微信小程序
    hospital-web/            # 医院后台
    api/                     # NestJS模块化单体
  packages/
    api-contracts/           # OpenAPI生成类型/Schema
    domain-types/            # 共享领域枚举和值对象
    ui/                      # Web共享UI，不强行复用小程序组件
    observability/           # 日志、指标和追踪
  services/
    ai-worker/               # 达到拆分条件后启用
  infra/
    docker/
    migrations/
    deployment/
  docs/
    architecture/
    api/
    security/
    runbooks/
```

后端模块示例：

```text
apps/api/src/modules/
  identity/
  organization/
  pet/
  template/
  care-plan/
  task/
  observation/
  timeline/
  collaboration/
  hospital-care/
  consent/
  ai-orchestrator/
  report/
  notification/
  audit/
  analytics/
```

---

## 18. 分阶段技术实施

### 阶段0：两周Concierge验证

目标不是写完整系统，而是验证数据结构和流程。

- Figma或轻量H5模拟宠主流程；
- 表单/小程序原型采集每日记录；
- 人工审核AI结构化结果；
- 由兽医评估复诊摘要；
- 固化第一版Observation Schema和报告结构。

交付物：流程原型、脱敏样本、AI评测集、数据字典和验证结论。

### 阶段1：MVP核心闭环，建议8—10周

#### Sprint 1：基础设施与身份

- 项目骨架、CI/CD和环境；
- 微信登录、医院账号和RBAC；
- 宠物档案和照护圈；
- 基础审计和对象存储；
- 统一AnalyticsEvent、实验分组和服务端埋点基础设施。

#### Sprint 2：计划和任务

- 模板、计划版本和用户确认；
- 用户自主计划、医院计划覆盖及冲突展示；
- 任务调度、完成、跳过和更正；
- 多人领取、防重复和提醒。

#### Sprint 3：状态与时间线

- 状态定义和记录；
- 动态照护卡聚合读模型；
- ClinicalEvent和完整时间线筛选；
- 媒体上传；
- 时间线投影；
- 基础趋势计算。

#### Sprint 4：AI与报告

- 语音转写和结构化提取；
- 用户确认；
- 周报和复诊摘要；
- 证据回溯和安全过滤。

#### Sprint 5：医院闭环与试点

- 患者邀请、执行概览和随访队列；
- 问卷提交、员工领取、复诊安排和计划更新闭环；
- 授权、撤销和访问审计；
- 试点指标看板、最小付费实验、性能和安全加固。

### 阶段2：产品验证后

- OCR导入；
- 医院自定义模板；
- 生活质量和照护负担；
- 更丰富的多模态输入；
- 正式订阅计费、退款和财务对账；
- 基础PIMS/硬件适配。

### 阶段3：规模化

- 将AI Worker、通知和报告按负载拆分；
- 事件总线切换为托管消息队列；
- 多区域容灾和更严格SLO；
- 标准化医院接口、设备接口和数据治理；
- 经授权的匿名化研究数据管道。

---

## 19. 团队与角色建议

### MVP研发团队

| 角色 | 建议人数 | 主要职责 |
| --- | ---: | --- |
| 产品负责人 | 1 | 需求、试点、指标和医疗边界 |
| 技术负责人/后端 | 1 | 架构、领域模型、安全和代码质量 |
| 后端工程师 | 1—2 | 业务模块、任务、报告和集成 |
| 小程序工程师 | 1 | 宠主端体验和微信能力 |
| Web前端工程师 | 1 | 医院后台 |
| AI工程师 | 1 | 提取、评测、Prompt和安全链路 |
| 测试工程师 | 1 | E2E、回归、并发和安全测试 |
| 设计师 | 0.5—1 | 小程序、医院后台和可用性 |
| 兽医顾问 | 0.5 | 模板、紧急规则和输出审核 |

小团队可由技术负责人兼后端、设计师兼职，但AI评测和兽医审核不可省略。

---

## 20. 工程验收标准

首阶段上线前至少满足：

1. 医院计划具备来源、版本和用户确认记录；
2. 任务生成具备幂等性，计划变更不会产生重复任务；
3. 两名家庭成员并发操作不会重复完成同一用药任务；
4. AI提取未经确认不会写入正式时间线；
5. 所有摘要结论均能跳转原始证据；
6. 趋势数值由确定性代码生成，而非LLM计算；
7. 医院访问实时校验授权状态、时间范围、数据类型、指标和媒体范围；
8. 分享链接可过期、撤销且不可枚举；
9. 关键操作和医院访问完整写入审计日志；
10. 紧急提示不承诺医院已接收或实时处理；
11. 核心E2E流程自动化通过；
12. 数据备份恢复演练成功；
13. AI医疗越界评测达到上线阈值；
14. 生产日志不存在Token和原始敏感内容；
15. 关键指标和告警可在监控面板查看；
16. 计划容器与计划版本状态独立，新版本待确认期间旧版本继续有效，确认后任务切换符合时间边界；
17. 用户自主创建首项任务后，动态照护卡可在一次请求中返回可执行内容；
18. 已完成用药再次操作必须二次确认并追加记录，不覆盖原执行；
19. 随访能够完成发送、提交、领取、完成、复诊和计划更新闭环；
20. 撤销授权后，家庭报告、证据、媒体、导出、分享链接和缓存均不可继续访问；
21. 原记录更正或隐藏后，趋势重新计算，相关报告标记STALE；
22. 能按PRD定义重算激活、留存、任务完成、AI修改、报告查看和随访指标；
23. 基础用药防重复协作不受付费权益限制。

---

## 21. 上线前待确认事项

1. 首批云区域、数据保留期及隐私条款；
2. 微信订阅消息可用模板和触达限制；
3. 医院员工角色、服务时间和响应责任文案；
4. 首批模板的专业审核人和版本发布流程；
5. 紧急规则的适用范围及法律审查；
6. AI模型供应商、境内数据处理和降级策略；
7. C端与医院端的计费、退款和合同边界；
8. 用户注销、数据导出、删除和医院记录保留关系；
9. 试点医院是否需要品牌定制；
10. AI输入是否允许用于模型改进，默认建议不允许，除非另行明确授权；
11. P0临时权限仅面向已有家庭成员，还是允许独立临时照护者通过邀请加入；
12. 医院依法或依约形成记录的具体范围，以及用户解除绑定后的展示和保留方式；
13. 患者面板“长期未更新”“即将复诊”和队列优先级的首批默认阈值。

---

## 22. PRD V3.0 P0追踪矩阵

| PRD能力 | 主要技术落点 | 核心验收 |
| --- | --- | --- |
| 动态照护卡 | CareCard聚合读模型、计划/任务/观察/复诊投影 | 一次请求返回当前可执行内容 |
| 照护计划 | CarePlan容器、PlanVersion、模板快照、Outbox | 来源可见，版本切换不重复任务 |
| 今日任务 | TaskDefinition/Instance/Execution、展示态计算 | 并发不重复，延迟/跳过/异常可追溯 |
| 低负担记录 | ObservationDefinition/Record、AI提取 | 正常与未记录分离，AI结果需确认 |
| 时间线 | ClinicalEvent事实、TimelineEvent投影 | 支持类型、来源、人员和异常筛选 |
| AI助手 | Schema提取、确定性趋势、证据包、失效重算 | 不越界，每项结论可追溯 |
| 周报与复诊摘要 | Report快照、多格式渲染、STALE机制 | 数据范围和完整度明确，分享可撤销 |
| 照护圈 | PetMember细粒度权限、数据库条件更新 | 防重复协作免费且权限可到期 |
| 医院连接 | PatientBinding、ConsentGrant、访问审计 | 绑定不等于授权或实时监控 |
| 医院患者面板 | Followup队列、版本化运营规则 | 分类可解释且不表达医疗风险 |
| 模板库 | TemplateVersion审核状态机和计划快照 | 未发布模板不能正式下发 |
| MVP验证 | AnalyticsEvent、统一指标口径、最小付费订单 | 可按试点分组重算PRD成功指标 |

---

## 23. 技术结论

暖宠的核心技术难点不是搭建普通提醒应用，而是同时保证：

- 多病种计划可以配置而不复制业务；
- 用药和多人协作具有事务一致性；
- 连续记录可以被计算、追溯和纠正；
- AI降低输入和整理成本但不越过医疗边界；
- 家庭数据在明确授权下安全地流向医院；
- 医院获得价值但不被默认绑定实时响应责任。

因此首阶段应采用模块化单体、关系数据库、确定性任务与趋势引擎，以及受Schema和规则约束的AI编排。先完成一个可靠、可审计的连续照护闭环，再依据真实负载和组织边界拆分服务，是风险最低且最符合当前产品阶段的工程路径。
