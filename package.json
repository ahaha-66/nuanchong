{
  "name": "nuanchong",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.34.4",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:integration": "pnpm --filter @nuanchong/api test:integration",
    "db:generate": "pnpm --filter @nuanchong/api prisma:generate",
    "db:migrate": "pnpm --filter @nuanchong/api prisma:migrate",
    "db:seed": "pnpm --filter @nuanchong/api prisma:seed"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@prisma/client", "@prisma/engines", "argon2", "esbuild", "prisma"]
  }
}
